ALU

-   TO DO:
    -   Pipelined (add a register at output)
    -   Add more ops (NOR, NAND, modulus,...)
    -   Make ALUControl one-hot instead of binary encoding
    -   Connect it into a mini CPU datapath